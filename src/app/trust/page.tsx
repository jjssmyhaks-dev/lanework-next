"use client"; import Link from "next/link"; import { ArrowRight, ShieldCheck, FileText, Eye, Lock, Undo2, ArrowLeft } from "lucide-react";
export default function TrustPage() {
  return (<main className="min-h-screen bg-white text-[#1a1a2e]"><header className="sticky top-0 z-40 border-b border-[#e5e7eb]/60 bg-white/80 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-4"><Link href="/" className="flex items-center gap-2"><div className="grid h-7 w-7 place-items-center rounded-md bg-[#1a1a2e]"><div className="h-3 w-3 rounded-sm bg-[#93c5fd]" style={{transform:"rotate(45deg)"}} /></div><span className="text-lg font-semibold">Lanework</span></Link><Link href="/register" className="rounded-full bg-[#1a1a2e] px-4 py-2 text-sm font-medium text-white hover:bg-[#1a1a2e]/90">Start Free Trial <ArrowRight className="inline h-3.5 w-3.5 ml-1" /></Link></div></header>
  <div className="mx-auto max-w-4xl px-6 py-16"><h1 className="text-5xl font-semibold">Trust &amp; Safety</h1><p className="mt-4 text-lg text-[#1a1a2e]/60">How Lanework keeps your operations safe, auditable, and under your control.</p>
  <div className="grid gap-6 md:grid-cols-2 mt-12">
    {[{Icon:ShieldCheck,title:"Full Audit Trail",desc:"Every autonomous action is logged with a timestamp, the agent that took it, the data it saw, and the reasoning it used. Exportable as CSV or PDF."},{Icon:Eye,title:"Configurable Approval Thresholds",desc:"Set per-agent, per-action type thresholds. Anything above the threshold is proposed for human review instead of auto-executed."},{Icon:Lock,title:"Tenant-Isolated Data",desc:"Your data is stored in a dedicated schema. Never shared across customers. Never used to train shared models. Encryption at rest and in transit."},{Icon:Undo2,title:"Human Override, Always Available",desc:"Every agent action has a one-click undo. You can reverse any decision, tighten the rules, and the agent learns from the correction."},{Icon:FileText,title:"Compliance Ready",desc:"Audit logs meet SOC 2 requirements. GDPR-compliant data handling. Regular penetration testing and vulnerability scanning."},{Icon:ShieldCheck,title:"Agent Guardrails",desc:"Every agent operates within hard limits: maximum spend per action, maximum shipments affected, geofence constraints. You set the boundaries."}].map(({Icon,title,desc})=>(<div key={title} className="flex gap-4 p-5 rounded-xl border border-[#e5e7eb]"><Icon className="h-6 w-6 text-[#1a1a2e] mt-0.5 flex-shrink-0" /><div><div className="font-semibold">{title}</div><div className="text-sm text-[#1a1a2e]/60 mt-1">{desc}</div></div></div>))}
  </div></div></main>);
}