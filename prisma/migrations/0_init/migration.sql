-- Baseline migration
-- This marks the existing database schema as version 0.
-- The schema was originally created via `prisma db push`.
-- All future changes should use `npx prisma migrate dev <name>`.

-- No SQL needed — this migration exists only to establish the baseline.
-- To apply: `npx prisma migrate deploy` (marks as applied without running SQL).
